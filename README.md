# slopymemory
Per-project memory for coding agents: one launcher, one registry, a store per project.
Install: `./install.sh` (Linux, no root; needs `git`, `curl` and `iproute2` from the system; asks before each step — `SETUP.md` explains every step and every failure).
