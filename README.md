# slopymemory
Per-project memory for coding agents: one launcher, one registry, a store per project.
Install: `./install.sh` (Linux, no root; asks before each step — `SETUP.md` explains every step and every failure).
