# The name the launcher is registered under and announces itself as. Not the generic `memory`: the reference
# knowledge-graph server and other memory servers are commonly registered as that, and a second `add` under it
# would fail or overwrite theirs.
SERVER_NAME = "slopymemory"
